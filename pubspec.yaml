name: library_app
description: Offline-first library management app
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.24.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
